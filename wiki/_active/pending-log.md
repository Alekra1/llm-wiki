# Pending log entries

Auto-generated drafts from the SessionEnd hook. **Not** the source of truth.

**On session start**, the agent reviews entries here, promotes worthwhile ones
into `log.md` with the proper category (decision / pitfall / preference /
workflow / session-end), then deletes the candidates from this file.

Empty file = nothing to review.
